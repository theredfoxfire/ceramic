{% extends 'baseAdmin.html.twig' %}
{% form_theme form 'bootstrap_3_layout.html.twig' %}
{% block body %}
    <h1>User creation</h1>

    {{ form_start(form) }}
        {{ form_widget(form) }}<br />
        <input type="submit" class="btn btn-info" value="Create" />
    {{ form_end(form) }}

            <br><a href="{{ path('unites_index') }}">Back to the list</a><br><br>

{% endblock %}
